<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MSBuildWorkspaceTester.ViewModels"
        mc:Ignorable="d"
        Title="MSBuildWorkspace Tester" Height="600" Width="800"
        vm:ViewModel.RegisterCommands="{Binding}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0" ToolBarTray.IsLocked="True">
            <ToolBar>
                <Button Command="{Binding OpenProjectCommand}" ToolTip="Open project or solution">
                    <Image Source="{StaticResource OpenFolderIcon}" />
                </Button>
            </ToolBar>
        </ToolBarTray>

        <TextBox Grid.Row="1"
                 FontFamily="Consolas"
                 FontSize="14.0"
                 Text="{Binding OutputText, Mode=OneWay}"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
